name: dudv_base
description: Model–view–presenter (MVP) is a derivation of the model–view–controller (MVC) architectural pattern, and is used mostly for building user interfaces.
version: 1.2.0+4
homepage: https://github.com/beheobong/dudv_base

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  permission_handler: ^10.2.0
  fluttertoast: ^8.2.1
  logger: ^1.2.2

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  analyzer: ^5.6.0


assets:
    - assets/icons_ios/`
  
